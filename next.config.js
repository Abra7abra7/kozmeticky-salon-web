/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['picsum.photos', 'via.placeholder.com', 'images.unsplash.com', 'example.com'],
  },
};

module.exports = nextConfig;
